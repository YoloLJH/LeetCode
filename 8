#LeetCode 80.删除排序数组中的重复项二
class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        i = 0;
        for n in range(0,len(nums)):
            if i<2 or nums[n]>nums[i-2]:
                nums[i] = nums[n]
                i=i+1
        return i
